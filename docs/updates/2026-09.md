# 2026-09 update log

Index and query commands: [README.md](README.md). New entries go at the end.

---

## U-20260922-01 · 2026-09-22 · Adopt progress/architecture/docs-updates rules · #docs #migration

- **What**: `CLAUDE.md` gained the workspace rule section (commit at every stage; `progress.md` holds open items only; finished work is recorded in `docs/updates/`; `architecture.md` is the short overview). Added `architecture.md` and this `docs/updates/` index. `progress.md` starts with the open items found in the workspace inventory of 2026-09-22 (`D:\Codes\docs\updates\2026-09.md`, U-20260922-01..05). Added `CLAUDE.md` (there was none).
- **Files**: `CLAUDE.md`, `progress.md`, `architecture.md`, `docs/updates/`.
- **Open items**: see `progress.md`.

## U-20260922-02 · 2026-09-22 · README with the plugin list, install steps and the guide link · #done #docs

- **What**: added `README.md` (English and Traditional Chinese): the six plugins with their files, highlighted suffixes and run support (read from each file's `register()` and `PLUGIN_RUN_CONFIG`), installing through *Plugins → Plugin Browser* or by hand, and rules for contributing. It links to JEditor's `PLUGIN_GUIDE.md`, now the single guide for JEditor and PyBreeze, which in turn names this repository instead of the nonexistent `exe/jeditor_plugins/` (JEDITOR U-20260922-04, PyBreeze U-20260922-08). The Plugin Browser skips `README.md` by name, so it does not show up as a plugin. Closes `progress.md` #1 and #2 (workspace X-10).
- **Files**: `README.md`, `progress.md`.
- **Open items**: none.
